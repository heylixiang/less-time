{
  "name": "less-time",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --project tsconfig.app.json --noEmit && tsc --project tsconfig.node.json --noEmit && vite build",
    "build:github-pages": "tsc --project tsconfig.app.json --noEmit && tsc --project tsconfig.node.json --noEmit && vite build --base=/less-time/",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
